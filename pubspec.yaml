name: Core-Vanta
description: Restro Pro POS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Bluetooth
  flutter_blue_plus: ^1.32.7
  permission_handler: ^11.3.1
  
  # PDF + Charts - LATEST. No conflict now
  pdf: ^3.11.3
  printing: ^5.12.2
  fl_chart: ^0.70.2
  image: ^4.3.0
  
  # Other
  shared_preferences: ^2.2.3
  intl: ^0.20.3
  sqflite: ^2.4.2
  path: ^1.9.1
  google_fonts: ^6.2.1
  path_provider: ^2.1.6
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
